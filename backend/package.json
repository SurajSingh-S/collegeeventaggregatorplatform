{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend for college events platform",
  "main": "server.js",
  "scripts": {
    "install": "cd frontend && npm install && cd ../ && npm install --prefix backend",
    "build": "npm run build --prefix frontend",
    "start": "node server.js",
    "heroku-postbuild": "npm run build"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cloudinary": "^1.41.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.3",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
